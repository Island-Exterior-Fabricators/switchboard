{
  "name": "switchboard",
  "version": "2.0.0",
  "description": "MCP relay server — lets multiple Claude Code instances and Claude Desktop message each other",
  "type": "module",
  "main": "relay-mcp.js",
  "scripts": {
    "start": "node relay-mcp.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.22.0"
  }
}
